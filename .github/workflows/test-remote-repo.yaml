name: SCI008-SCA-Scanning
on:
  workflow_dispatch:
    inputs:
      FEATURE_BRANCH_NAME:
      description: 'Freature Branch name that need to be scanned'
      required: false
      type: string
      default: main
    
jobs:
  test:
    - name: my test app
      run: echo "sanitized_branch=${inputs.FEATURE_BRANCH_NAME/\//-}"
